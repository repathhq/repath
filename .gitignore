# Context (internal research docs)
context/

# Rust
/target
# Cargo.lock is intentionally committed for binary applications (reproducible builds)
**/*.rs.bk

# Environment
.env
.env.local
.env.*.local
*.env
!.env.example

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code local state (worktrees can contain stale copies of old,
# already-redacted secrets — never let this get committed)
.claude/

# macOS
.DS_Store

# Logs
*.log
logs/

# Database
*.db
*.sqlite

# Node (for dashboard)
dashboard/node_modules/
dashboard/.next/
out/
dist/
build/

# Python (for evaluators)
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.coverage
htmlcov/
.venv/
venv/
env/

# Docker
docker-compose.override.yml

# Terraform
*.tfstate
*.tfstate.backup
.terraform/
*.tfvars
*.tfvars.json
tfplan
*.tfplan

# Temporary files
tmp/
temp/
inspirations/