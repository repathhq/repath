name: Sync Dashboard to Personal Repo

on:
  workflow_dispatch:  # Manual trigger only — use scripts/sync-dashboard.sh locally instead

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use scripts/sync-dashboard.sh locally to sync the dashboard."
